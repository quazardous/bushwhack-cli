# LF everywhere, whatever core.autocrlf says: bin/bushwhack and setup.sh are run by bash,
# which a carriage return breaks. The Windows scripts keep CRLF.
* text=auto eol=lf
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf
